# Binaries
bin/
dist/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go
vendor/
*.test
*.out
go.work
go.work.sum

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment and secrets
.env
.env.*
!.env.example
config.yaml
config.local.yaml

# Data
data/
*.db
*.db-shm
*.db-wal
*.log
coverage/

# Build artifacts
tmp/
*.o
*.a

# OMC / AI tooling state
.omc/
.omx/
.claude/
.opencode/

# Playwright / Browser
.playwright-mcp/

# Frontend build
web/node_modules/
web/dist/
web/.vite/
web/tsconfig*.tsbuildinfo

# Embedded frontend assets (generated by `make web-build`).
# `internal/web/dist/index.html` is tracked as a stub so `go build`
# works without Node.js; everything else under dist/ is built output.
internal/web/dist/assets/
internal/web/dist/*.js
internal/web/dist/*.css
internal/web/dist/*.svg
internal/web/dist/*.png
internal/web/dist/*.ico
internal/web/dist/*.map
internal/web/dist/.vite/

# Design system source (not part of the build)
Anchored OSS/
Anchored OSS.zip

# Screenshots from dev
*.png
!web/public/*.png

# Sisyphus state
.sisyphus/

# Compiled binary at repo root
/server
